ansible==14.3.1
# Needed for community.crypto module
cryptography==50.0.1
# Needed for jinja2 json_query templating
jmespath==1.1.0
# Needed for ansible.utils.ipaddr
netaddr==1.3.0
# Needed for community.routeros collection
librouteros==4.2.0
# Needed for the password_hash Jinja filter (setup_users role)
passlib==1.7.4
# ansible-galaxy
distlib==0.4.3
# CHANGELOG generation (used by pre-push hook)
git-changelog==2.9.7

