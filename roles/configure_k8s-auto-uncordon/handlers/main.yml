---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart uncordon-k8s-nodes timer
  ansible.builtin.service:
    name: uncordon-k8s-nodes.timer
    state: restarted
  become: true
