---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-kecskemethy
  namespace: traefik
spec:
  secretName: wildcard-tls
  dnsNames:
    - "*.kecskemethy.org"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
