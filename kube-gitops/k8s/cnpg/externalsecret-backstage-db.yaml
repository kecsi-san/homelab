---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backstage-db-credentials
  namespace: postgres
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-homelab
    kind: ClusterSecretStore
  target:
    name: backstage-db-credentials
  data:
    - secretKey: username
      remoteRef:
        key: k8s/db/backstage
        property: username
    - secretKey: password
      remoteRef:
        key: k8s/db/backstage
        property: password
