# REQUIRED before cnpg-cluster will bootstrap successfully.
# Run once, then commit the generated file over this placeholder:
#
# PGPASS="$(openssl rand -base64 24)"
# echo "Forgejo DB password: $PGPASS  — save this, you need it again below"
#
# kubectl create secret generic forgejo-db-credentials \
#   --namespace postgres \
#   --from-literal=password="$PGPASS" \
#   --dry-run=client -o yaml | \
#   kubeseal --format yaml \
#     --context "admin@k8s" \
#     --controller-name sealed-secrets \
#     --controller-namespace sealed-secrets \
#   > kube-gitops/k8s/cnpg/sealedsecret-forgejo-db.yaml
