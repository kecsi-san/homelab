---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headlamp-tls
  namespace: kube-system
spec:
  secretName: headlamp-tls
  dnsNames:
    - "headlamp.<your-domain.tld>"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
