# REQUIRED — same password as kube-gitops/k8s/cnpg/sealedsecret-forgejo-db.yaml.
# Use the $PGPASS value you saved when creating the cnpg secret:
#
# kubectl create secret generic forgejo-db-credentials \
#   --namespace forgejo \
#   --from-literal=password="$PGPASS" \
#   --dry-run=client -o yaml | \
#   kubeseal --format yaml \
#     --context "admin@k8s" \
#     --controller-name sealed-secrets \
#     --controller-namespace sealed-secrets \
#   > kube-gitops/k8s/forgejo/sealedsecret-db.yaml
