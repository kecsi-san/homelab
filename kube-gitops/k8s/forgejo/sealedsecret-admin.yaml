# REQUIRED — Forgejo web UI admin credentials.
# username / password / email keys required by the Gitea chart existingSecret.
#
# kubectl create secret generic forgejo-admin-credentials \
#   --namespace forgejo \
#   --from-literal=username='admin' \
#   --from-literal=password='<YOUR_ADMIN_PASSWORD>' \
#   --from-literal=email='kecskemethy@gmail.com' \
#   --dry-run=client -o yaml | \
#   kubeseal --format yaml \
#     --context "admin@k8s" \
#     --controller-name sealed-secrets \
#     --controller-namespace sealed-secrets \
#   > kube-gitops/k8s/forgejo/sealedsecret-admin.yaml
