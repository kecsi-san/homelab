---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-wikijs-oauth2
  namespace: authentik
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-homelab
    kind: ClusterSecretStore
  target:
    name: authentik-wikijs-oauth2
  data:
    - secretKey: client-secret
      remoteRef:
        key: k8s/oauth2/wikijs
        property: client-secret
