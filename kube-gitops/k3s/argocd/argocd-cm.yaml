---
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
data:
  url: https://argocd.k3s.kecskemethy.org
  resource.customizations.knownTypeFields.apps_Deployment: |
    - field: "status.terminatingReplicas"
      type: "integer"
  resource.customizations.knownTypeFields.apps_ReplicaSet: |
    - field: "status.terminatingReplicas"
      type: "integer"
  resource.customizations.knownTypeFields.apps_StatefulSet: |
    - field: "status.terminatingReplicas"
      type: "integer"
  resource.customizations.knownTypeFields.apps_DaemonSet: |
    - field: "status.terminatingReplicas"
      type: "integer"
  resource.customizations.ignoreDifferences.apps_Deployment: |
    jqPathExpressions:
      - .status.terminatingReplicas
  resource.customizations.ignoreDifferences.apps_ReplicaSet: |
    jqPathExpressions:
      - .status.terminatingReplicas
  resource.customizations.ignoreDifferences.apps_StatefulSet: |
    jqPathExpressions:
      - .status.terminatingReplicas
  resource.customizations.ignoreDifferences.apps_DaemonSet: |
    jqPathExpressions:
      - .status.terminatingReplicas
  oidc.config: |
    name: Authentik
    issuer: https://authentik.k3s.kecskemethy.org/application/o/argocd-k3s/
    clientID: argocd-k3s
    clientSecret: $argocd-oidc-secret:client-secret
    requestedScopes:
      - openid
      - profile
      - email
      - groups
  # Everything below here is the argo-cd Helm chart's own default argocd-cm
  # content (chart 10.8.1, captured via `helm template`). It moved here
  # because `configs.cm.create: false` in roles/setup_argocd (see TODO.md's
  # "Helm 4 migration" item) makes this manifest the sole owner of the
  # ConfigMap instead of co-owning it with Helm — Helm 4's strict
  # Server-Side-Apply ownership model doesn't tolerate two writers on the
  # same object the way Helm 3's lenient 3-way merge did. Kept verbatim so
  # disabling the chart's own copy doesn't silently drop any of it.
  admin.enabled: "true"
  application.instanceLabelKey: argocd.argoproj.io/instance
  application.sync.impersonation.enabled: "false"
  exec.enabled: "false"
  statusbadge.enabled: "false"
  timeout.reconciliation: 120s
  timeout.reconciliation.jitter: 60s
  timeout.hard.reconciliation: 0s
  resource.customizations.ignoreResourceUpdates.all: |
    jsonPointers:
      - /status
  resource.customizations.ignoreResourceUpdates.ConfigMap: |
    jqPathExpressions:
      # Ignore the cluster-autoscaler status
      - '.metadata.annotations."cluster-autoscaler.kubernetes.io/last-updated"'
      # Ignore the annotation of the legacy Leases election
      - '.metadata.annotations."control-plane.alpha.kubernetes.io/leader"'
  resource.customizations.ignoreResourceUpdates.Endpoints: |
    jsonPointers:
      - /metadata
      - /subsets
  resource.customizations.ignoreResourceUpdates.discovery.k8s.io_EndpointSlice: |
    jsonPointers:
      - /metadata
      - /endpoints
      - /ports
  resource.customizations.ignoreResourceUpdates.apps_ReplicaSet: |
    jqPathExpressions:
      - '.metadata.annotations."deployment.kubernetes.io/desired-replicas"'
      - '.metadata.annotations."deployment.kubernetes.io/max-replicas"'
      - '.metadata.annotations."rollout.argoproj.io/desired-replicas"'
  resource.customizations.ignoreResourceUpdates.autoscaling_HorizontalPodAutoscaler: |
    jqPathExpressions:
      - '.metadata.annotations."autoscaling.alpha.kubernetes.io/behavior"'
      - '.metadata.annotations."autoscaling.alpha.kubernetes.io/conditions"'
      - '.metadata.annotations."autoscaling.alpha.kubernetes.io/metrics"'
      - '.metadata.annotations."autoscaling.alpha.kubernetes.io/current-metrics"'
  resource.customizations.ignoreResourceUpdates.argoproj.io_Application: |
    jqPathExpressions:
      - '.metadata.annotations."notified.notifications.argoproj.io"'
      - '.metadata.annotations."argocd.argoproj.io/refresh"'
      - '.metadata.annotations."argocd.argoproj.io/hydrate"'
      - '.operation'
  resource.customizations.ignoreResourceUpdates.argoproj.io_Rollout: |
    jqPathExpressions:
      - '.metadata.annotations."notified.notifications.argoproj.io"'
  resource.exclusions: |
    ### Network resources created by the Kubernetes control plane and excluded to reduce the number of watched events and UI clutter
    - apiGroups:
      - ''
      - discovery.k8s.io
      kinds:
      - Endpoints
      - EndpointSlice
    ### Internal Kubernetes resources excluded reduce the number of watched events
    - apiGroups:
      - coordination.k8s.io
      kinds:
      - Lease
    ### Internal Kubernetes Authz/Authn resources excluded reduce the number of watched events
    - apiGroups:
      - authentication.k8s.io
      - authorization.k8s.io
      kinds:
      - SelfSubjectReview
      - TokenReview
      - LocalSubjectAccessReview
      - SelfSubjectAccessReview
      - SelfSubjectRulesReview
      - SubjectAccessReview
    ### Intermediate Certificate Request excluded reduce the number of watched events
    - apiGroups:
      - certificates.k8s.io
      kinds:
      - CertificateSigningRequest
    - apiGroups:
      - cert-manager.io
      kinds:
      - CertificateRequest
    ### Cilium internal resources excluded reduce the number of watched events and UI Clutter
    - apiGroups:
      - cilium.io
      kinds:
      - CiliumIdentity
      - CiliumEndpoint
      - CiliumEndpointSlice
    ### Kyverno intermediate and reporting resources excluded reduce the number of watched events and improve performance
    - apiGroups:
      - kyverno.io
      - reports.kyverno.io
      - wgpolicyk8s.io
      kinds:
      - PolicyReport
      - ClusterPolicyReport
      - EphemeralReport
      - ClusterEphemeralReport
      - AdmissionReport
      - ClusterAdmissionReport
      - BackgroundScanReport
      - ClusterBackgroundScanReport
      - UpdateRequest
