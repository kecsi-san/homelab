---
- name: Configure MikroTik router (DNS records + NAT rules)
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"

  roles:
    - role: configure_mikrotik-router
      tags: [mikrotik, mikrotik-dns, mikrotik-nat]
